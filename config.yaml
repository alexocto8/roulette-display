# Configuração do painel de roleta. Editável aqui ou pelo menu de administração (CTRL+ALT+A).
casino_name: "Jackpot Poker Club"
roulette_name: "ROLETA 01"
roulette_id: 1

history_size: 15
statistics_window: 300
hot_numbers_count: 5
cold_numbers_count: 5

currency: "R$"
min_bet: "5,00"
max_bet: "5.000,00"

fullscreen: true
target_fps: 30
idle_fps: 8
hide_cursor: true
# Rotação por software do conteúdo, pra quando a TV é montada em pé (retrato) mas o driver de
# vídeo não gira sozinho. Valores: 0 (nenhuma), 90, 180, 270. Padrão de fábrica: 90 (retrato --
# o layout principal é pensado pra TV montada em pé). Se a tela aparecer de cabeça para baixo
# ou de lado errado nessa instalação, troque para 270 -- depende só do sentido físico da
# montagem, não dá pra adivinhar de antemão.
screen_rotation: 90

admin_pin: "1234"
undo_confirm_seconds: 4.0

database_path: "data/roulette.db"
logs_dir: "logs"
assets_dir: "assets"
backups_dir: "data/backups"
exports_dir: "data/exports"

log_level: "INFO"
log_max_bytes: 2000000
log_backup_count: 5
